{% extends 'base.html.twig' %}

{% block title %}SubstanceCategory{% endblock %}

{% block body %}
    <h1>SubstanceCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ substance_category.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ substance_category.color }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ substance_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_substance_category_index') }}">back to list</a>

    <a href="{{ path('app_substance_category_edit', {'id': substance_category.id}) }}">edit</a>

    {{ include('substance_category/_delete_form.html.twig') }}
{% endblock %}
